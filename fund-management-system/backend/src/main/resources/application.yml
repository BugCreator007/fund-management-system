server:
  port: 8080

spring:
  application:
    name: fund-management-backend
  
  datasource:
    url: jdbc:mysql://localhost:3306/funddb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: gulu
    password: Gxl@123@
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  h2:
    console:
      enabled: true
      path: /h2-console

# CORS配置
cors:
  allowed-origins: http://localhost:3000
